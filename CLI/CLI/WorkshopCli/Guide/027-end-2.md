[color=white]
Agora vamos fazer o Boss aparecer no ecra.
Ele so deve aparecer depois de derrotares alguns inimigos.

1. Primeiro, procura a funcao:
   [/color] [color=blue]
    function love.draw()
   [/color] [color=white]
2. Dentro dessa funcao, vai para perto do fim.
Cola este codigo antes dos ultimos "end" da funcao:
   [/color] [color=blue]
    if boss then
        boss:draw()
        love.graphics.print("-- Vida do Boss: " .. boss.vida .. " --", love.graphics.getWidth() / 2, 10)
    end
   [/color] [color=white]
Isto quer dizer: se o Boss existir, desenha o Boss e mostra a vida dele.

3. Agora procura a funcao:
   [/color] [color=blue]
    function love.update(dt)
   [/color] [color=white]
4. Dentro dessa funcao, procura esta linha:
   [/color] [color=blue]
    atualizarInimigos(dt)
   [/color] [color=white]
5. Logo por baixo dessa linha, cola este codigo:
   [/color] [color=blue]
    if inimigosDerrotados >= bossApareceApos and boss == nil then
        criaBoss()
    end
    if boss then
        boss:update(dt)
    end
   [/color] [color=white]
Isto quer dizer:

- Se ja derrotaste inimigos suficientes e ainda nao existe Boss, cria o Boss.
- Se o Boss ja existe, atualiza o movimento dele.

6. Agora procura a funcao:
   [/color] [color=blue]
    function verificaMissilInimigoColisao()
   [/color] [color=white]
7. Dentro dessa funcao, procura esta linha:
   [/color] [color=blue]
    table.remove(misseis, index2)
   [/color] [color=white]
8. Logo por baixo dessa linha, cola isto:
   [/color] [color=blue]
    inimigosDerrotados = inimigosDerrotados + 1
   [/color] [color=white]
Isto aumenta a contagem sempre que derrotas um inimigo.

Atencao:

- O codigo do Boss no "draw" fica dentro de "love.draw()".
- O codigo que cria o Boss fica dentro de "love.update(dt)".
- A contagem dos inimigos fica dentro da colisao missil-inimigo.

Guarda e testa com Alt + L.
Depois de derrotares 5 inimigos, o Boss deve aparecer.
[/color]
